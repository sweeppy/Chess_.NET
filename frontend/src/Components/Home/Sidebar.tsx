const Sidebar = () => {
  return (
    <div
      data-column
      className="sidebar inline-padding padding-block flex-center flow"
    >
      <h2 className="inline-padding">Get better with pixel chess online!</h2>
      <button className="primary-button max-width">Sign in</button>
      <button className="primary-button max-width">Sign up</button>
      <div className="flex-center"></div>
    </div>
  );
};

export default Sidebar;
